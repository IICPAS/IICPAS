import React from "react";
import Header from "../components/Header";
import LoginPage from "./LoginPage";

const page = () => {
  return (
    <div>
      <Header />
      <LoginPage />
    </div>
  );
};

export default page;
